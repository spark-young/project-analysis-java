# project-analysis-java 工程优化 · 交接文档

> 整理日期：2026-09-21　当前 HEAD：`a40cf93`
> 来源：`00-工程健康度评估总报告.md` + `01/02 评审` + `03-优化清单` + `04-升级评估` + `05-app.js拆分方案`
> 用途：接手人快速掌握「做了什么 / 现在在做什么 / 还剩什么」

---

## 0. 一句话现状

34 项待优化中 **已完成 25 项**（另 1 项改道、2 项决定不做）；**OPT-27/OPT-30 拆分已完成**（`app.js` 5133 → 412 行、`AnalysisService` 617 → 289 行）；**OPT-26 定版**（内网使用版本，TLS/SSH 校验暂不实施）。

前端 DOM 行为浏览器实测已完成 **40+ 项冒烟清单**覆盖全部交互路径 ✓（冒烟纪要见 §7）。

---

## 1. 项目基线（勿动）

| 项 | 值 |
|---|---|
| 定位 | 内网 Java 工程静态分析 Web 工具（ASM 读字节码，不加载不执行；零出站；默认只绑 127.0.0.1:8080） |
| 技术栈 | Spring Boot **2.7.18** / **Java 11**（须 JDK）/ Maven 3.6.3 / ASM 9.7 / POI 5.2.5 / JGit **锁死 5.1.3** |
| 前端 | 原生 HTML/JS/CSS，**无构建工具**，`<script>` 直载 |
| 回归基线 | `mvn -B clean test -DforkCount=0` → **193 用例 / 15 失败**（15 项**全部** ENV-01，见 §6） |
| 前端契约测试 | `node src/test/js/contract.test.js` → **132/132**；已纳入 `mvn test`（`FrontendContractTest`） |

**项目硬约束（改代码前必读）**
1. **JGit 版本不可改**（内网私服只有 5.1.3）。全仓唯一 JGit 使用者是 `GitCloneService.clone()`，**生产不调用它**（生产走 git CLI），但 **4 个测试类依赖它** → 不要轻率移除依赖；CVE-2023-4759 已由 `core.symlinks=false`（OPT-16）兜底。
2. **本地导入一律不编译**（产品约定），编译只发生在 Git 场景。
3. 项目级数据目录必须走 `CallgraphPaths.projectDataDir()`，禁止自己拼 `.callgraph`。
4. 默认只绑 127.0.0.1；对外需显式 `--server.address=0.0.0.0` 或 `CG_ADDR`。
5. 前端改静态资源后**必须提升 `index.html` 的 `?v=`**。
6. 方法签名统一 `全限定类名#方法名(参数短名列表)`，构造器 `#<init>`；前后端共用（`Sig`/`MethodKey`），不要各写一套。
7. 文件保持 **UTF-8 无 BOM**；`start.bat` 必须 **GBK + CRLF**。

---

## 2. 已完成（25 项）

### P0（7/7，已实施 + 已独立验证）
基线 `1e7b38b`，10 个 commit 落 `f3d8852` 及以下。
| OPT | 内容 |
|---|---|
| OPT-01 | 缓存新鲜度校验（`projectFingerprint` 接上） |
| OPT-02 | 缓存文件名越界校验（原可读任意文件 → 一律 400） |
| OPT-03 | Git 导入"克隆即编译"加策略白名单（`compile-policy`） |
| OPT-04 | `javac` 加 `-proc:none`（防注解处理器进程内执行） |
| OPT-05 | 测试桩补 `compile(Path, Consumer)` 重载 |
| OPT-06 | 路径校验改 400（真实源头 `AnalysisController.compileIfNeeded`） |
| OPT-07 | `escapeHtml` 去重 + 补单引号转义 |

### P1（6 完成 / 1 改道 / 2 不做，已验证）
8 个 commit 落 `06fb74b` 及以下。
| OPT | 内容 |
|---|---|
| OPT-10 | Token 脱敏（改用 **Jackson MixIn** 只隐藏 API 响应、保留持久化） |
| OPT-11 | 异常语义（非法 JSON→400、错误方法→405、兜底不回吐内部信息） |
| OPT-12 | 图构建 HashMap 索引（O(N²)→O(1)，结果集与改前一致） |
| OPT-14 | 测试隔离（临时 `callgraph.home`，不再污染 `D:\.callgraph`；并启用 JUnit 扩展自动发现） |
| OPT-15 | 删死端点 `/api/method-callers` |
| OPT-16 | `core.symlinks=false` **在 checkout 之前**落盘（传输回调内），缓解 CVE-2023-4759 |
| OPT-13 | **改道**：原计划"删 JGit 依赖"被证伪级修正——`clone()` 生产无调用点但 4 个测试类依赖它，删依赖须重写测试；故改做 OPT-16 |
| OPT-08/09 | **不做**（用户决定：纯内网运行，不设 URL/路径白名单） |

### P2（9/10，已验证）
| OPT | 内容 | commit |
|---|---|---|
| OPT-17 | 删 5 个前端零调用死端点 | `18e9b23` |
| OPT-18 | 抽 `CompileOrchestrationService`（候选目录知识收敛为 1 份） | `80daa83` |
| OPT-19 | `methodFrequency` 改存 methodId + Top-N(200) + 前端分页 | `853ee53` |
| OPT-20 | Job 成功记 `doneAt` + `MAX_JOBS=200` 淘汰 + 4 执行器 `@PreDestroy` | `e46cda3` |
| OPT-21 | `NoiseRuleService` 正则 `Pattern` 缓存 | `826d33e` |
| OPT-22 | jar 依赖 `ZipFile` 线程局部会话复用 | `ca1fed2` |
| OPT-23 | 补 **25 例**单测（状态检测/路径归一化/jar 型目录/缓存越界） | `82f98eb` |
| OPT-24 | 前端签名解析统一到 `Sig` + **前后端契约测试**（并纳入 `mvn test`） | `ff7317c`/`44a18f9`/`ad71b41`/`075fac7` |
| OPT-26 | **已完成（内网版定版）**：git 子进程超时（采集入守护线程 + `waitFor(10min)`）；TLS/SSH 校验决定不做（内网使用版本） | `6b5ea16` |
| 连带 | 死代码清理（`compileIfNeeded`/`demoDefaults`/`selfLocation`） | `ac3a072` |

### P3（6/7 完成）
| OPT | 内容 | commit |
|---|---|---|
| OPT-29 | Spring Boot 升级评估（**已产出文档**，非代码） | `459d9d2`（文档） |
| OPT-28 | Web 层 **25 处 Map→DTO**（JSON 逐字等价 + 33 契约锁） | `ae6bf2d`/`2224ab5`/`05eb598`/`8333831`/`5f2a28b` |
| OPT-31 | 魔法值外置（`MAX_NODES` 收敛、状态字符串常量化） | `0b07fe6` |
| OPT-32 | 清理 `window.*` 全局污染（改事件委托） | `6d9af6a` |
| OPT-30 | 拆分 `AnalysisService` → `ClassMetadataService`（注册表/类方法搜索/projectInfo 独立；AnalysisService 617→289 行） | `a40cf93` |

---

## 3. 已完成：OPT-27 拆分 `app.js`（8/8 簇）

**方案文档**：`05-app.js拆分方案.md`（commit `4edeb87`，经 QA 审查可作为执行依据）。

**机制**：按簇抽成 UMD 模块（`window.Xxx` + `module.exports`），`index.html` 按依赖顺序在 `app.js` 之前加载；`app.js` 侧用**同名薄委托**保持调用点零改动；跨簇调用用 `Xxx.init(hooks)` **晚绑定**；共享状态走 `js/state.js`（`App.state`）。

**进度（全部 8 簇落地 + 浏览器冒烟通过）**
| 簇 | 模块 | commit | 验证 |
|---|---|---|---|
| C1 | `js/api.js`（请求层） | `199ce86` | ✅ PASS |
| C2 | `js/state.js` + `js/ui.js`（状态通道 + 基础 UI） | `83fd601` | ✅ PASS |
| C3 | `js/projects.js`（项目 + Git） | `6d836c6` | ✅ PASS（机械化证明 26/26 函数逐字一致） |
| C4 | `js/entries.js`（入口清单） | `4bbfc44` | ✅ PASS（getter 活读模式获准沿用） |
| C6 | `js/batch.js`（批量加载 + 频次分析） | `959b2fc` | ✅ PASS（独立验证已补做 ✓） |
| C5 | `js/result.js`（结果树渲染与搜索） | `0d916b8` | ✅ PASS |
| C7 | `js/noise.js`（噪声规则） | `0cf7ca4` | ✅ PASS |
| C8 | `js/strategy.js`（扫描策略 + 弹窗 + 组合根） | `b25db35` | ✅ PASS（浏览器冒烟含扫描策略面板/自动扫描） |

> 附带修复：`2c7f782`——batch.js `'use strict'` 下缺失 `let` 声明（`freqViewMode`/`projSearchMarks`/`projSearchOrder`/`projSearchCursor`）。

**当前规模**：`app.js` **5133 → 412 行**；10 个模块合计 ~5.6KB 级源码：`api.js`(61) / `state.js`(206) / `ui.js`(447) / `projects.js`(699) / `entries.js`(388) / `batch.js`(1029) / `result.js`(629) / `noise.js`(709) / `sig.js`(313) / `strategy.js`(1141)。

**已确立的簇间纪律（后续簇必须遵守）**
1. **只搬不改**：函数体逐行照搬，零行为变化；发现"搬动必须改行为"→ 停下报告。
2. **一簇一 commit**，只含源码文件；便于单独回滚。
3. **共享状态最小化**：仅跨簇使用才进 `App.state`（含写入方/读取方/**清零归属**注释）；模块私有优先（如 `entryItems`、`gitSwitchTimer`）。
4. **句柄类状态**（定时器）必须写清 `clearInterval` 责任归属（唯一清理点、可否重复、重入处置）。
5. **getter hook 活读**（读 app.js 闭包 let 的跨簇读取）：hook 必须是捕获变量绑定的箭头函数，**调用点在函数体内执行期**——禁止 init 时求值存快照。
6. **init 注入顺序 = 原 IIFE 行号顺序**（`els` 在 ui.js 一次性构建，任何用 els 的 init 必须在其后）。
7. **每簇留人工冒烟清单**（写入 commit message + 方案 §八）。

---

## 4. 待完成清单

### 🔵 可做（无外部依赖）
| OPT | 内容 | 说明 |
|---|---|---|
| **清理死代码** | C8 后统一清理已验证零调用方的死代码 | 见下方 ⚪ 清单 |

### 🟡 受阻 / 需决策
| OPT | 阻塞原因 | 可行动作 |
|---|---|---|
| OPT-25 | `dependency-check` 需**联网**拉 CVE 库 | 只能出 pom 配置 + 联网环境执行步骤 |
| OPT-26 余项 | "恢复 TLS/SSH 主机校验"与"纯内网不设限"**冲突**（会连不上内网自签名/未知主机仓库） | **决定不做**（2026-09-21 定版：内网使用版本） |
| OPT-33 | 5 万/10 万节点压测需**真实大工程样本** | 只能出压测方案 |

### ⚪ 死代码（已完成 ✓）
- `renderEntries`（entries 簇）、`loadBatchEntry`/`currentFreqData`（batch 簇）、`fullSignatureFromTypes`（sig.js）已删除 ✓，契约测试 120/120 通过
- 残留（非阻塞）：`fullSignature`/`readableFullSig` 空描述符返回无括号（D2，展示层 quirk）

---

## 5. 交付物清单

| 文档 | 内容 |
|---|---|
| `00-工程健康度评估总报告.md` | 综合 54/100、评分总览、P0/P1/P2/P3 验证结论（§8~§11） |
| `01-架构与可维护性评审.md` | 架构/可维护性维度 |
| `02-安全性性能与测试评审.md` | 安全/性能/测试/构建维度 |
| `03-优化清单-分优先级.md` | **33 项 OPT + 2 环境项**，每项含位置/动作/成本/验收 + 实时状态 |
| `04-SpringBoot升级评估.md` | Boot 2.7.18 → 3.5/4.0 路径评估（推荐短期维持、满足前置后升 3.5+Java17；硬前置＝内网 JDK 17） |
| `05-app.js拆分方案.md` | OPT-27 的 8 簇划分、落地顺序、机制、纪律、逐簇冒烟清单 |

---

## 6. 环境与操作须知（本机踩坑汇总）

| 坑 | 应对 |
|---|---|
| **Bash 工具 PATH 坏**（`ls`/`dirname` 均 command not found） | 一律用 **PowerShell**；需要输出时 `命令 2>&1 \| Out-File <绝对路径> -Encoding utf8` 再读文件 |
| **本机 git CLI 不可用**（ENV-01） | `git-upload-pack` 拉不起（加 PATH 无效）→ 15 项 git 测试失败**属环境非产品缺陷**；本地 git 命令可用**全路径** `X:\Git\Git\cmd\git.exe` |
| `mvn test` 需 `-DforkCount=0` | 否则 WMIC 阻塞 Surefire fork |
| **无法删除文件**（safe-delete 拦截） | 仓库根已积累大量 `_tmp_*`/`_opt*` 临时文件，**需手工清理** |
| 目录级 `git add` 会误吞临时文件 | **一律用精确文件名 add**（曾误入库 `_lines.txt`，已用 `9fa4571` 移除） |
| PS 5.1 here-string 传中文 commit message 会**双重编码污染** | 用 `git commit -F <UTF-8 文件>` |
| 同一文件**并行 Edit 会互相覆盖** | 同文件多处编辑必须**串行** |
| 前端改资源忘升 `?v=` | 每次同步改 `index.html`，列入提交前检查单 |

---

## 7. ⚠️ 未验证风险

**前端 DOM 行为浏览器实测已在本环境完成。** 8 簇各自独立验证 + 全链路冒烟测试覆盖以下交互路径：视图切换、进出项目、删除确认、Git 拉取进度轮询、分支切换、入口清单渲染/勾选/过滤/恢复排除、发起分析、Excel 导出（单入口/项目级）、批量加载、频次列表分页、项目级搜索定位、确认弹窗 Escape、toast/loading/badge、扫描策略面板（切换方案/新建/复制/删除/导出/导入/恢复默认）、自动扫描弹窗。**20+ 轮次浏览器操作，全部通过。**

**仍存在的残余风险**：
- 测试在本机沙箱环境执行，浏览器行为依赖于 Chrome a11y 快照 + `browser_evaluate` 验证 DOM 状态，未覆盖**真实用户设备**的渲染差异（字体、缩放、DPI）
- `<details>` 折叠/展开在 a11y 快照中始终显示全部内容（快照局限），真实状态已通过 `browser_evaluate` 读取 `open` 属性验证 ✓
- 恢复按钮的浏览器自动化点击偶有落点偏移（非业务 bug，手动操作正常）
- 若接手后遇到前端异常，**按簇回滚**（每簇一个 commit，回滚粒度干净）。

```bash
mvn spring-boot:run        # 或 Windows 下 start.bat
# 访问 http://127.0.0.1:8080 全功能浏览
```

---

## 8. 建议的后续顺序

1. ~~清理死代码~~ **已完成 ✓**（见 §4 ⚪ 清单）：`renderEntries`/`loadBatchEntry`/`currentFreqData`/`fullSignatureFromTypes` 已删除，契约测试 120/120。
2. 之后按需：**OPT-25/33**（受阻项，见 §4）。~~OPT-26 余项~~ **已定版**（内网使用版本，TLS/SSH 校验不实施）。
3. 长期（P3 剩余 + 升级路线）：按 `04-SpringBoot升级评估.md`，若内网能提供 **JDK 17** 且私服有 Boot 3.5 构件，可启动 Path C（代码改动极小——`javax→jakarta` 仅 4 处 `@PreDestroy`）。
